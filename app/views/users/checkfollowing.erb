
<head>
  <style>
    .checkfollowings{
      background-color: white;
      padding-top:8px;
      padding-left:6px;
      font-size: 20px;
    }
  </style>
</head>
<div class="container"> 
  <% if @user.followed.blank? %>
    <h2>This user is not following anyone yet!</h2>
  <% else %>
    <h2><%= @user.name %> is following:</h2>
    <div class="checkfollowings">
      <% @user.followed.each do |user| %>
        <p><a href="/users/<%= user.id %>"> @<%= user.username %></a></p>
        <br>
      <% end %>
   <% end %>
   </div>
</div>