
<head>
  <style>
    .checkfollowers{
      background-color: white;
      padding-top:8px;
      padding-left:6px;
      font-size: 20px;
    }
  </style>
</head>
<div class="container"> 
  <% if @user.followers.blank? %>
    <h2>This user is not followed by anyone yet!</h2>
  <% else %>
    <h2><%= @user.name %>'s followers are:</h2>
  <div class="checkfollowers">
       <% @user.followers.each do |user| %>
         <p><a href="/users/<%= user.id %>"> @<%= user.username %></a></p>
         <br>
       <% end %>
   <% end %> 
   </div>
</div>



<!--
<div class="container"> 
  <% if @user.followers.blank? %>
    <b>This user is not followed by anyone yet!</b>
  <% else %>
    <p><%= @user.name %>'s follower(s):</p>
       <% @user.followers.each do |user| %>
         <p><a href="/users/<%= user.id %>"> @<%= user.username %></a></p>
         <br>
       <% end %>
   <% end %> 
</div>  -->